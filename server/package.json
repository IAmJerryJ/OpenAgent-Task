{
  "name": "server",
  "version": "1.0.0",
  "main": "src/server.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "start:dev": "node dist/server.js",
    "dev": "nodemon --watch src --ext ts --exec \"tsc && node dist/server.js\"",
    "dev:reset": "set SEED_MODE=reset&& nodemon --watch src --ext ts --exec \"tsc && node dist/server.js\"",
    "dev:build": "nodemon --exec \"tsc && node dist/server.js\"",
    "dev:build:reset": "set SEED_MODE=reset&& nodemon --exec \"tsc && node dist/server.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/pg": "^8.15.5",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.9.1",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
